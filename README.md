# BioArray Selector

Static browser app for selecting `RHD` and `RHCE` marker calls and previewing the interpreted output table.

## Local use

Open `rhd_variant_selector.html` directly in a browser.

## Deployed app

The production deployment serves `rhd_variant_selector.html` as the site index for `bioarray.bloodapps.com`.
